package Indriya.Core.Msgs;

/// \ingroup experimot_msgs
/// \interface Plugin
/// \brief A message containing visual information for experimot::Plugin

message Plugin
{
  required string name         = 1;
  required string filename     = 2;
  optional string innerxml     = 3 [default = ""];
}
