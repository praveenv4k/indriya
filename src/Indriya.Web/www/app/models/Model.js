// Model.js
// Praveenkumar VASUDEVAN
define(["jquery", "backbone"],
    function($, backbone) {
        var model = backbone.Model.extend({
            initialize: function() {
            },
            defaults: {
            
            },
            validate: function(attrs) {
            }
        });
        return model;
    });